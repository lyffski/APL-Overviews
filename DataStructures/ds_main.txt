DS is a way of organizing data do that it can be used effectively
ADT is an abstraction of a data structure which provides only the interface to which a data structure must adhere to

Complexity of Dynamic Array
  access O(1)
  search O(n)
  insertion O(n)
  appending O(1)
  deletion O(n)
